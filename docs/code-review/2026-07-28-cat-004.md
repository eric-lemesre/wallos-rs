# Revue de code — REQ-CAT-004 (unicité du nom de catégorie par foyer)

- **Date** : 2026-07-28
- **Commit revu** : `6ba2dca` (PR #48, fusionnée)
- **Revieweur** : kimi (`kimi -p`, post-merge)
- **Verdict global** : **aucun défaut CRITICAL/HIGH**. Unicité **par foyer** correcte (un autre foyer garde
  son homonyme), insensibilité à la casse via l'index DB, pas d'`unwrap` en production, `rename` distingue
  correctement `NotFound`/`Duplicate`, le trim en amont (`Category::new`) empêche le contournement par espaces.

## Traitement (consigne : traiter + annoter)

| # | Sévérité | Finding | Statut | Décision |
|---|----------|---------|--------|----------|
| 1 | MEDIUM | Divergence possible `Rust to_lowercase()` (dans `Category::canonical_name`) vs Postgres `lower()` (index unique) sur l'Unicode exotique (İ turc, NFC/NFD) | ✅ **CLARIFIÉ (pas un bug)** | `canonical_name` **n'est pas dans le chemin d'enforcement** : l'unicité est tranchée **exclusivement** par l'index DB `(household_id, lower(name))` (locale unique). Docstring rectifiée : le helper core est illustratif, la base fait autorité. Aucune comparaison d'unicité n'est faite en `core`, donc aucune divergence exploitable. |
| — | LOW | Contournement par espaces internes/finaux (« Streaming » vs « Streaming ») | ❌ **NON RETENU** | `Category::new` **trim** en amont (create + rename passent par lui) ; l'API ne peut pas créer de nom à espaces de bord. kimi le reconnaît « protégé en amont ». |
| — | LOW | Cas Unicode non testés dans le test unitaire core | ⏳ **NON RETENU** | La casse Unicode est gérée par `to_lowercase()` (illustratif) ; l'enforcement réel (DB) est couvert par les tests d'intégration (doublon rejeté, casse « streaming », autre foyer autorisé). |

**Bilan** : aucun défaut de correction. Le seul point (divergence lowercase) est neutralisé par conception
(enforcement 100 % DB) et désormais documenté. Isolation par foyer et insensibilité à la casse confirmées.
