# Revue de code — REQ-SUB-008 (abonnement désactivé exclu des agrégats)

- **Date** : 2026-07-28
- **Commit revu** : `e5e062d` (PR #42, fusionnée)
- **Revieweur** : kimi (`kimi -p`, post-merge)
- **Verdict global** : **aucun défaut CRITICAL**. La règle d'exclusion est correcte (`filter(active)` côté
  core et API), montants `Decimal`, isolation respectée. Findings surtout robustesse UI et couverture de test.

## Traitement (consigne : traiter + annoter)

Corrections appliquées dans la **PR SUB-011** (même module `SubscriptionsList.tsx` + tests d'intégration).

| # | Sévérité | Finding | Statut | Décision |
|---|----------|---------|--------|----------|
| 1 | HIGH | `save` ne capture pas une **exception réseau** du `PUT` (fetch rejeté → rejet non géré, éditeur bloqué sans message) | ✅ **CORRIGÉ** | `try/catch` autour du `PUT` : statut d'erreur **ou** exception réseau → `subscriptions-save-error`, mode édition maintenu. |
| 2 | MEDIUM | `billable_amounts` documenté « point de sélection unique » alors que `server::active_amounts` duplique la règle | ✅ **CORRIGÉ** | Docstring rectifiée : `billable_amounts` = primitive de domaine (STA) ; `active_amounts` = même règle au niveau des lignes stockées, référence croisée explicite. |
| 3 | MEDIUM | Test d'intégration ne couvre pas la **désactivation via PUT** ni l'état retourné | ✅ **CORRIGÉ** | Test `deactivating_then_reactivating_via_put_toggles_the_total` : PUT `active:false` → réponse `active:false` + total `0` + abonnement conservé ; PUT `active:true` → total rétabli. |
| 4 | MEDIUM | Cas de **réactivation** non testé | ✅ **CORRIGÉ** | Couvert par le même test (aller-retour désactivation → réactivation). |
| 5 | MEDIUM | `active_amounts` exclut silencieusement un montant/devise illisible sans marquer l'agrégat incomplet | ❌ **REJETÉ** | **Inatteignable** : migration `0008` impose `CHECK (amount >= 0)` et l'écriture valide la devise (référentiel via `CurrencyCode::new`). Un abonnement actif porte toujours un montant/devise lisible. |
| 6 | LOW | Assertion e2e sur le total « faible » / course du driver `deactivateSubscription` | ❌ **REJETÉ** | Déjà mitigé : le spec attend via `expect.poll(() => subscriptionsTotal())` que le total reflète l'exclusion. |

**Bilan** : 4 corrigés, 2 rejetés (garde inatteignable / déjà mitigé). Aucun défaut d'isolation ni de correction.
