# Revue de code — REQ-SUB-004 (modification d'un abonnement)

- **Date** : 2026-07-28
- **Commit revu** : `e5e22c3` (PR #41, fusionnée)
- **Revieweur** : kimi (`kimi -p`, post-merge)
- **Verdict global** : **aucun défaut CRITICAL**. Isolation de l'abonnement correcte (`UPDATE … WHERE id
  AND household_id`, `404` jamais `403`), aucun `unwrap`/`expect`/`panic` en production, montants `Decimal`.

## Traitement (consigne : traiter + annoter)

Corrections appliquées dans la **PR SUB-008** (même module `SubscriptionsList.tsx` + tests d'intégration).

| # | Sévérité | Finding | Statut | Décision |
|---|----------|---------|--------|----------|
| 1 | HIGH | Références `category_id` / `payment_method_id` / `payer_id` non validées comme appartenant au foyer lors de l'UPDATE (référence pendante possible vers une entité d'un autre foyer) | ⏳ **DIFFÉRÉ** | **Pas de fuite de confidentialité** : aucun `join` serveur, la liste ne renvoie que l'UUID brut (le nom de la catégorie/du payeur n'est jamais renvoyé). Gap **préexistant** (identique sur `createSubscription`). Le correctif complet exige les **référentiels payeur (SUB-017) et moyen de paiement (SUB-011) non encore construits** → à traiter **uniformément** (create + update, les 3 références) quand ils existeront. Noté au backlog transverse `.hermes`. |
| 2 | MEDIUM | UI : échec du `PUT` non signalé (rechargement silencieux) | ✅ **CORRIGÉ** | `save` teste `response.ok` ; en cas d'échec, `subscriptions-save-error` (alerte i18n) s'affiche et le mode édition **reste ouvert** pour correction. Test vitest ajouté. |
| 3 | MEDIUM | Cas limites non testés (id absent/malformé → 404) | ✅ **CORRIGÉ** | Test `update_unknown_or_invalid_id_is_404` (id absent valide **et** UUID malformé → `404`). |
| 4 | MEDIUM | `CreateSubscriptionRequest` réutilisé comme corps du `PUT` | ❌ **REJETÉ** | Choix **DRY délibéré** : le jeu de champs éditables est identique à la création ; un DTO distinct serait une duplication sans valeur. Documenté dans le handler. |
| 5 | LOW | Fallback silencieux `i32::try_from(...).unwrap_or(i32::MAX)` en base | ❌ **REJETÉ** | **Inatteignable** : `CreateSubscriptionRequest::into_core` **rejette déjà** tout `cycle.interval > i32::MAX` (correctif CRIT-1 de la revue SUB-002) et la migration `0008` ajoute `CHECK (cycle_interval > 0)`. Le fallback est un garde défensif jamais franchi. |

**Bilan** : 2 corrigés, 1 différé (dépend de référentiels non construits — sans impact confidentialité),
2 rejetés (choix de conception justifiés / garde inatteignable). Aucun défaut d'isolation ni de sécurité.
