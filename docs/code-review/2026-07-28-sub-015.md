# Revue de code — REQ-SUB-015 (logo d'abonnement, substitut local)

- **Date** : 2026-07-28
- **Commit revu** : `0ee2e81` (PR #46, fusionnée)
- **Revieweur** : kimi (`kimi -p`, post-merge)
- **Verdict global** : **aucun défaut CRITICAL/HIGH**. **Aucun appel réseau caché** : `logoSubstitute` est
  synchrone, sans `fetch` ; `SubscriptionLogo` n'émet rien quand le logo est absent — la confidentialité
  « par conception » (acceptance #1/#2) tient. Findings : robustesse des initiales et de l'affichage.

## Traitement (consigne : traiter + annoter)

Corrections appliquées dans la **PR SUB-009** (mêmes fichiers `logoSubstitute`/`SubscriptionLogo`).

| # | Sévérité | Finding | Statut | Décision |
|---|----------|---------|--------|----------|
| 1 | MEDIUM | Initiales cassées sur emoji / hors-BMP (`w[0]` = unité UTF-16 → demi-surrogate) | ✅ **CORRIGÉ** | `String.fromCodePoint(w.codePointAt(0))` — préserve emoji/hors-BMP. Test ajouté (`🎵 Music` → `🎵M`, `Ötzi` → `Ö`). |
| 2 | MEDIUM | Logo « blanc » (espaces) truthy → `<img src=" ">` au lieu du substitut | ✅ **CORRIGÉ** | `if (logo?.trim())`. Test ajouté (`logo="   "` → substitut). |
| 3 | LOW | Substitut sans dimensions ni couleur de texte garanties (incohérence visuelle vs `<img>`) | ✅ **CORRIGÉ** | Styles inline : `32×32`, `inline-flex` centré, texte blanc. |
| 6 | LOW | Contraste WCAG limite sur certaines teintes à `L=45 %` | ✅ **CORRIGÉ** | `L` abaissé à `40 %` + texte blanc. |
| 5 | LOW | Cas limites non testés (nom vide exact `""`, emoji, hors-BMP) | ✅ **CORRIGÉ** | Assertions ajoutées (`""` → `?`, emoji, `Ö`). |
| 4 | LOW | Pas de repli si l'image distante échoue à charger (`onError`) | ⏳ **DIFFÉRÉ** | Nécessite un composant à état ; nice-to-have (bascule sur le substitut si l'URL casse). Backlog durcissement UI. |

**Bilan** : 5 corrigés (initiales Unicode, logo blanc, styles/contraste, tests), 1 différé (`onError` image).
Confidentialité confirmée : aucun `fetch` de logo, substitut purement local.
