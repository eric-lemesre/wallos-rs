# Revue de code — REQ-SUB-011 (moyen de paiement)

- **Date** : 2026-07-28
- **Commit revu** : `c23e3b3` (PR #43, fusionnée)
- **Revieweur** : kimi (`kimi -p`, post-merge)
- **Verdict global** : **aucun défaut CRITICAL**. Pas de faille d'isolation, pas d'injection SQL, pas
  d'`unwrap`/`panic` en production. Findings : intégrité référentielle (thème transverse) et robustesse UI.

## Traitement (consigne : traiter + annoter)

Corrections rapides appliquées dans la **PR CUR-001** (mêmes fichiers de test) ; le reste tracé au backlog.

| # | Sévérité | Finding | Statut | Décision |
|---|----------|---------|--------|----------|
| 1 | HIGH | Pas de FK `subscriptions.payment_method_id → payment_methods` : suppression d'un moyen référencé laisse un UUID mort | ⏳ **DIFFÉRÉ** | **Thème transverse d'intégrité référentielle** déjà au backlog (revue SUB-004 #1). Couplé à la validation d'appartenance au foyer (create/update) et au payeur (SUB-017). À traiter **uniformément** (category + payer + payment_method, FK + validation 422 plutôt que 500) dans une passe dédiée. Aucune fuite ni crash (UUID simplement stocké/affiché). |
| 2 | HIGH | UI `PaymentMethodsList` : create/rename/remove ignorent la réponse (pas de feedback d'erreur, champ vidé avant confirmation) | ⏳ **DIFFÉRÉ** | Comportement **identique à `CategoriesList`** (calque, déjà mergé sous CAT-001). Passe de **durcissement UI dédiée** aux deux composants (gestion d'erreur de mutation + état). Pas une régression de correction. |
| 3 | MEDIUM | Pas de test de suppression d'un moyen référencé | ⏳ **DIFFÉRÉ** | Le comportement dépend de #1 (FK) ; à spécifier avec la passe d'intégrité référentielle. |
| 4 | MEDIUM | Cas limites non testés (nom > 100, UUID malformé rename/delete) | ✅ **CORRIGÉ** | Test `edge_cases_are_rejected` : nom > 100 → 422, UUID malformé rename/delete → 404. |
| 5 | MEDIUM | Pas d'état de chargement (double-clic → appels multiples) | ⏳ **DIFFÉRÉ** | Idem #2 (durcissement UI, aussi valable pour `CategoriesList`). |
| 6 | LOW | `failed` ne couvre que le chargement initial, pas les mutations | ⏳ **DIFFÉRÉ** | Idem #2. |

**Bilan** : 1 corrigé (cas limites), 5 différés (intégrité référentielle transverse + durcissement UI
commun à `CategoriesList`). Aucun défaut d'isolation ni de sécurité. Items tracés dans `.hermes`.
