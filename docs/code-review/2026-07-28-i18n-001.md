# Revue de code — REQ-I18N-001 (choix et persistance de la langue)

- **Date** : 2026-07-28
- **Commit revu** : `ac6e3d5` (PR #45, fusionnée)
- **Revieweur** : kimi (`kimi -p`, post-merge)
- **Verdict global** : **aucun défaut CRITICAL/HIGH**. Isolation par `user_id` correcte, pas d'injection,
  pas d'`unwrap` en production, langue non supportée rejetée. Findings : robustesse du repli système (UI).

## Traitement (consigne : traiter + annoter)

Corrections appliquées dans la **PR SUB-015** (même composant `LanguageSetting.tsx` + tests settings).

| # | Sévérité | Finding | Statut | Décision |
|---|----------|---------|--------|----------|
| 1 | MEDIUM | Course : un `GET` de montage tardif réapplique l'ancienne langue après un choix utilisateur | ✅ **CORRIGÉ** | Garde `chosen` (`useRef`) : le `GET` n'écrase jamais un choix déjà fait. |
| 2 | MEDIUM | Pas de **repli système** si le `GET` échoue (401 avant connexion, 500) → contredit l'acceptance #2 | ✅ **CORRIGÉ** | Le repli `systemLanguage()` s'applique désormais **indépendamment** du succès du `GET` (persisté sinon système). Test ajouté (`navigator.language = fr-FR` + `GET` vide → i18n `fr`). |
| 3 | LOW | Flash initial en anglais avant repli système (`current` = `"en"` codé en dur) | ✅ **CORRIGÉ** | `current` initialisé à `systemLanguage() ?? i18n.language`. |
| 4 | LOW | Liste `SUPPORTED` codée en dur dans l'UI (à aligner manuellement avec `core::Language` + CHECK SQL) | ⏳ **DIFFÉRÉ** | Endpoint `/settings/supported-languages` (ou enum OpenAPI) dans une passe ultérieure ; liste de 2 codes stable, alignée sur `core::Language::ALL`. |
| 5 | LOW | Cas limites API non testés (casse `FR`, vide, code long, locale) | ✅ **CORRIGÉ** | Test paramétré : `de`, `FR`, `""`, `english`, `fr-FR` → tous `422`. |
| 6 | LOW | `get_language` → `500` si la ligne utilisateur disparaît (vs `401` de `getCurrentUser`) | ⏳ **DIFFÉRÉ** | Edge (compte supprimé + session vivante). Passe de cohérence du mapping d'erreurs (aligner sur `getCurrentUser`). |

**Bilan** : 4 corrigés (repli système robuste — complète l'acceptance #2 — + garde de course + cas limites),
2 différés (endpoint langues supportées, mapping 401 sur compte supprimé). Aucun défaut d'isolation.
