# Revue de code — REQ-SUB-006 (liste et filtres d'abonnements)

- **Date** : 2026-07-28
- **Commit revu** : `ba192f1` (PR #40, fusionnée)
- **Revieweur** : kimi (`kimi -p`, en arrière-plan post-merge)
- **Verdict global** : **aucun défaut CRITICAL/HIGH**. Isolation stricte correcte (`where household_id = $1`,
  tests owner/other/anon), pas d'injection SQL (paramètres liés), filtres conjonctifs corrects, inactifs
  exclus du total, montants en `Decimal` (R4), aucun `unwrap` paniquable (R5). 8 findings MEDIUM/LOW/NIT.

## Traitement (consigne : traiter + annoter)

Corrections appliquées dans la **PR SUB-004** (même module `crates/server/src/subscriptions.rs` +
`SubscriptionsList.tsx` — regroupement naturel), sauf indication contraire.

| # | Sévérité | Finding | Statut | Correctif |
|---|----------|---------|--------|-----------|
| 1 | MEDIUM | Filtre **payeur** absent de l'UI (l'acceptance cite « catégories, payeurs et états » ; l'API le supporte déjà) | ✅ **CORRIGÉ** | Champ `subscriptions-filter-payer` ajouté à `SubscriptionsList` (+ i18n en/fr), envoyé dans `query.payer` ; filtres conjonctifs. |
| 2 | MEDIUM | Montant négatif / devise invalide **en base** exclus silencieusement du total (pas de CHECK) | ✅ **CORRIGÉ** | Migration `0008_subscriptions_integrity.sql` : `CHECK (amount >= 0)` + `CHECK (cycle_interval > 0)` (défense en profondeur ; le domaine validait déjà à l'écriture). |
| 3 | MEDIUM | DTO incohérent : `interval=0` affiché (unwrap_or_default) alors que `row_cycle=None` abandonne `next_payment` | ✅ **CORRIGÉ** | `row_to_dto` dérive le cycle **une seule fois** via `row_cycle` ; `interval` et `next_payment` restent cohérents. |
| 4 | LOW | `today.pred_opt().unwrap_or(today)` saute l'occurrence k=0 si `today == NaiveDate::MIN` | ❌ **REJETÉ** | `Utc::now()` n'est jamais proche de `NaiveDate::MIN` (année 0) ; le correctif proposé (`+1 jour`) changerait la sémantique de référence. Cas impossible en pratique. |
| 5 | LOW | Tests ne couvrent pas le filtre `active=false` seul | ✅ **CORRIGÉ** | Test `active_filter_selects_inactive_only_with_zero_total` : `?active=false` → uniquement l'inactif, total `0`. |
| 6 | LOW | Tests ne couvrent pas le filtre `payer` | ✅ **CORRIGÉ** | Test `payer_filter_selects_matching_subscriptions`. |
| 7 | NIT | Index manquants sur les colonnes de filtre | ✅ **CORRIGÉ** | Migration `0008` : index `(household_id, category_id)`, `(household_id, payer_id)`, `(household_id, active)`. |
| 8 | NIT | `load_rate_table` appelé même quand `amounts` est vide (aller-retour base inutile) | ✅ **CORRIGÉ** | Garde `if amounts.is_empty()` → `RateTable::new(vec![])` sans requête. |

**Bilan** : 7 corrigés, 1 rejeté (cas impossible). Aucun défaut d'isolation ni de sécurité — la revue
confirme la conformité §9 que SUB-002 réclamait pour la lecture.
