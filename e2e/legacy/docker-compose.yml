# Cible legacy figée : Wallos 5.4.2, épinglée par DIGEST (ADR 0011). Ce fichier est la source unique
# du pin ; toute montée exige un nouvel ADR + rejeu des oracles (§8.1). Le digest est immuable, le tag
# ne l'est pas — c'est le digest qui fait foi.
#
# Base éphémère (aucun volume persistant) : chaque conteneur repart de l'état initial de l'image, ce
# qui rend les scénarios reproductibles. Le LegacyDriver s'inscrit au 1er run puis se connecte.

services:
  wallos:
    image: bellamy/wallos:5.4.2@sha256:316f26e13265958e7946ef98ff600516fddc51d698ee98bd1ae1577e5e00789f
    ports:
      - "8282:80"
