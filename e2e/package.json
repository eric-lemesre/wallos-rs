{
  "name": "@wallos/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "e2e:legacy": "playwright test --config playwright.legacy.config.ts",
    "e2e:record": "RECORD=1 playwright test --config playwright.legacy.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.7.3"
  }
}
